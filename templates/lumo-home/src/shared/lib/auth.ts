// import { AuthClient } from 'payload-auth-plugin/client'

// export const appAuthClient = new AuthClient('app', {
//   payloadBaseURL: process.env.NEXT_PUBLIC_PAYLOAD_URL,
// })
// export const adminAuthClient = new AuthClient('admin', {
//   payloadBaseURL: process.env.NEXT_PUBLIC_PAYLOAD_URL,
// })
