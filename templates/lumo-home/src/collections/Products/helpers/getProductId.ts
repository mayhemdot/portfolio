// import { Product } from '@/payload-types'

// export const getProductId = (
//   product: Product | number | undefined | null,
// ): number | undefined | null =>
//   typeof product === 'object' && product !== null ? product.id : product
